namespace MMV.Application.UseCases.Customers.ListCustomersForPicker;

/// <summary>
/// Entrée (query) du use case <see cref="ListCustomersForPickerUseCase"/> — « Lister les clients pour un sélecteur »
/// (P2D-6). Cette lecture n'a pas de critère (le formulaire de commande charge tous les clients puis filtre en
/// présentation, iso-fonctionnel). Objet conservé pour l'homogénéité des query use cases et la garde « query nulle ».
/// </summary>
public sealed class ListCustomersForPickerQuery
{
}
