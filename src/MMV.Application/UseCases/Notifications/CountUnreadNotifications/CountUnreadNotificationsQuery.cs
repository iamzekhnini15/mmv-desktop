namespace MMV.Application.UseCases.Notifications.CountUnreadNotifications;

/// <summary>
/// Entrée (query) du use case <see cref="CountUnreadNotificationsUseCase"/> — « Compter les notifications non
/// lues » (P2D-3). Aucun critère : la query existe pour la cohérence de convention et la garde « query nulle ».
/// </summary>
public sealed class CountUnreadNotificationsQuery
{
}
