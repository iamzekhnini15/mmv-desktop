namespace MMV.Application.UseCases.Notifications.MarkAllNotificationsRead;

/// <summary>
/// Sortie (DTO) du use case <see cref="MarkAllNotificationsReadUseCase"/>. Neutre : le flux d'origine ne renvoyait
/// aucune donnée (la ViewModel se contentait de recharger la liste).
/// </summary>
public sealed class MarkAllNotificationsReadResult
{
}
