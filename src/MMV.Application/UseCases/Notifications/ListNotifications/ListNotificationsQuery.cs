namespace MMV.Application.UseCases.Notifications.ListNotifications;

/// <summary>
/// Entrée (query) du use case <see cref="ListNotificationsUseCase"/> — « Lister les notifications » (P2D-3).
/// Renvoie la liste complète, triée par date de création décroissante (comme à l'affichage historique).
/// </summary>
public sealed class ListNotificationsQuery
{
}
