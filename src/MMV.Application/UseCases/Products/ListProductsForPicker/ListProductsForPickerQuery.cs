namespace MMV.Application.UseCases.Products.ListProductsForPicker;

/// <summary>
/// Entrée (query) du use case <see cref="ListProductsForPickerUseCase"/> — « Lister les produits pour un sélecteur »
/// (P2D-4). Alimente les sélecteurs produit du module Stock (filtre de la liste des mouvements et lignes du
/// formulaire de mouvement). La recherche reste en présentation : la query renvoie la liste complète, triée par nom.
/// </summary>
public sealed class ListProductsForPickerQuery
{
}
